# Copy to .env and edit for your environment
PORT=5321

# MSSQL connection
DB_SERVER=localhost
DB_PORT=1433
DB_NAME=UserActivityDB
DB_USER=sa
DB_PASSWORD=C0nn3ct@123
DB_ENCRYPT=false
DB_TRUST_SERVER_CERTIFICATE=true

# Shared secret the agent must send in the X-Agent-Key header.
# Change this in production.
AGENT_SHARED_KEY=change-me-in-production

# CORS - comma separated list of allowed origins for the dashboard
CORS_ORIGINS=http://localhost:5173,http://localhost:3000
